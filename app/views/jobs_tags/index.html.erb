<h1>Listing jobs_tags</h1>

<table>
  <tr>
    <th>Tag</th>
    <th>Job</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @jobs_tags.each do |jobs_tag| %>
  <tr>
    <td><%= jobs_tag.tag_id %></td>
    <td><%= jobs_tag.job_id %></td>
    <td><%= link_to 'Show', jobs_tag %></td>
    <td><%= link_to 'Edit', edit_jobs_tag_path(jobs_tag) %></td>
    <td><%= link_to 'Destroy', jobs_tag, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Jobs tag', new_jobs_tag_path %>
