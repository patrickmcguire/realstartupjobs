<%= form_for(@saved_company) do |f| %>
  <% if @saved_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@saved_company.errors.count, "error") %> prohibited this saved_company from being saved:</h2>

      <ul>
      <% @saved_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br />
    <%= f.number_field :company_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
