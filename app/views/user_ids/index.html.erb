<h1>Listing user_ids</h1>

<table>
  <tr>
    <th>Company</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_ids.each do |user_id| %>
  <tr>
    <td><%= user_id.company_id %></td>
    <td><%= link_to 'Show', user_id %></td>
    <td><%= link_to 'Edit', edit_user_id_path(user_id) %></td>
    <td><%= link_to 'Destroy', user_id, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_id_path %>
